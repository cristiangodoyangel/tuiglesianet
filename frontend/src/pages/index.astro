---
import MainLayout from "../layouts/MainLayout.astro";
import Dashboard from "../components/Dashboard.jsx";
import {
  Book,
  BookA,
  BookAIcon,
  BookCheckIcon,
  BookOpenText,
  CalendarClockIcon,
} from "lucide-react";
---

<MainLayout title="Panel General">
  <div class="mb-8 p-4 bg-accent/30 rounded-lg border border-accent">
    <p class="text-primary font-medium"></p>
  </div>

  <Dashboard client:load />

  <div class="mt-8 grid grid-cols-1 lg:grid-cols-2 gap-6">
    <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
      <div class="flex items-center">
        <CalendarClockIcon ClassName: w-13 h-13 />
        <h3 class="font-bold text-primary mb-2 text-lg ml-2">
          Actividades de la Semana
        </h3>
      </div>
      <ul class="space-y-3 mt-4">
        <li
          class="flex items-center gap-3 text-sm p-3 hover:bg-gray-50 rounded-lg transition-colors border border-transparent hover:border-gray-100"
        >
          <div
            class="flex flex-col items-center justify-center w-12 h-12 bg-blue-50 text-blue-600 rounded-lg font-bold text-xs"
          >
            <span>DOM</span>
            <span class="text-base">10</span>
          </div>
          <div>
            <p class="font-semibold text-gray-800">Culto Dominical</p>
            <p class="text-xs text-gray-500">10:00 AM - Templo Principal</p>
          </div>
        </li>
        <li
          class="flex items-center gap-3 text-sm p-3 hover:bg-gray-50 rounded-lg transition-colors border border-transparent hover:border-gray-100"
        >
          <div
            class="flex flex-col items-center justify-center w-12 h-12 bg-green-50 text-green-600 rounded-lg font-bold text-xs"
          >
            <span>VIE</span>
            <span class="text-base">15</span>
          </div>
          <div>
            <p class="font-semibold text-gray-800">Reunión de Jóvenes</p>
            <p class="text-xs text-gray-500">19:00 PM - Salón Multiuso</p>
          </div>
        </li>
      </ul>
    </div>

    <div class="p-6 rounded-xl shadow-lg text-white relative overflow-hidden">
      <div
        class="absolute top-0 right-0 -mt-10 -mr-10 w-40 h-40 bg-[#cea14d] rounded-full blur-3xl opacity-20"
      >
      </div>

      <h3 class="font-bold text-primary mb-4 text-lg relative z-10">
        <BookOpenText ClassName: w-13 h-13 /> 📖 Versículo del Día
      </h3>
      <blockquote class="italic text-gray-300 text-lg relative z-10">
        "Todo lo puedo en Cristo que me fortalece."
      </blockquote>
      <p class="text-right text-[#cea14d] font-bold mt-4 relative z-10">
        - Filipenses 4:13
      </p>
    </div>
  </div>
</MainLayout>
